<div id='customerRacketModels'>
  <%= select_tag :racket_id,
                 options_from_collection_for_select([Racket.new(:model => 'Select', :id => -1)].concat(racket_models), :id, :full_name),
                 :onchange => 'updateRacketModelDetails(this)'%>
</div>
